import React from "react";

const AboutReact = () => {
  return (
    <div style={{ padding: " 20px" }}>
      <h2>What is React?</h2>
      <p>
        React is a front-end JavaScript library developed by Facebook in 2011.
        It follows the component-based approach that helps in building reusable
        UI components. It is used for developing complex and interactive web and
        mobile UI. Even though it was open-sourced only in 2015, it has one of
        the largest communities supporting it.
      </p>

      <h2>
        What are the features of React?{" "}
        <span role="img" aria-label="features">
          🤔
        </span>
      </h2>

      <p>
        <strong>JSX:</strong> JSX is a syntax that is used to write HTML within
        JavaScript. It makes it easier to write and add HTML in React.
      </p>
      <p>
        <strong>Components:</strong> React is all about components. You need to
        create small components that are reusable and composable.
      </p>
      <p>
        <strong>Unidirectional data flow:</strong> React follows the
        unidirectional data flow or one-way data binding.
      </p>
      <p>
        <strong>Single-way data flow:</strong> React follows the single-way data
        flow or one-way data binding.
      </p>
      <p>
        <strong>Virtual DOM:</strong> React creates an in-memory data structure
        cache, computes the resulting differences, and then updates the
        browser's displayed DOM efficiently.
      </p>
      <p>
        <strong>Performance:</strong> React uses a virtual DOM that makes the
        user experience better and the developer's work faster.
      </p>
      <p>
        <strong>JSX:</strong> JSX is a syntax that is used to write HTML within
        JavaScript. It makes it easier to write and add HTML in React.
      </p>
      <p>
        <strong>Components:</strong> React is all about components. You need to
        create small components that are reusable and composable.
      </p>
      <p>
        <strong>Unidirectional data flow:</strong> React follows the
        unidirectional data flow or one-way data binding.
      </p>
      <p>
        <strong>Single-way data flow:</strong> React follows the single-way data
        flow or one-way data binding.
      </p>
      <p>
        <strong>Virtual DOM:</strong> React creates an in-memory data structure
        cache, computes the resulting differences, and then updates the
        browser's displayed DOM efficiently.
      </p>
      <p>
        <strong>Performance:</strong> React uses a virtual DOM that makes the
        user experience better and the developer's work faster.
      </p>
      <p>
        <strong>JSX:</strong> JSX is a syntax that is used to write HTML within
        JavaScript. It makes it easier to write and add HTML in React.
      </p>

      <h2>What is advantages of React? </h2>

      <ul>
        <li>Easy to learn</li>
        <li>Reusable components</li>
        <li>Performance</li>
        <li>Virtual DOM</li>
        <li>One-way data binding</li>
        <li>JSX</li>
        <li>Community support</li>
      </ul>

      <h2>What is disadvantages of React? </h2>

      <ul>
        <li>JSX can be a little difficult</li>
        <li>Too many small components</li>
        <li>View part</li>
        <li>React is just a library</li>
        <li>Too much boilerplate</li>
        <li>Complex to understand</li>
        <li>Poor documentation</li>
      </ul>

      <h2>differences between React and Angular? </h2>

      <ul>
        <li>React is a library, Angular is a framework</li>
        <li>React uses virtual DOM, Angular uses real DOM</li>
        <li>React uses JSX, Angular uses HTML and TypeScript</li>
        <li>React is lightweight, Angular is heavy</li>
        <li>React is easier to learn, Angular is harder to learn</li>
        <li>React is more flexible, Angular is less flexible</li>
        <li>React is more popular, Angular is less popular</li>
      </ul>

      <h2>Why use React instead of other frameworks, like Angular? </h2>

      <p>
        React is a library, Angular is a framework. React uses virtual DOM,
        Angular uses real DOM. React uses JSX, Angular uses HTML and TypeScript.
        React is lightweight, Angular is heavy. React is easier to learn,
        Angular is harder to learn. React is more flexible, Angular is less
        flexible. React is more popular, Angular is less popular.
      </p>
      <h2>What is the difference between the ES6 and ES5 standards? </h2>

      <p>
        ES5 is the fifth version of the ECMAScript standard, and ES6 is the
        sixth version of the ECMAScript standard. ES6 is also known as
        ECMAScript 2015.
      </p>
    </div>
  );
};

export default AboutReact;
